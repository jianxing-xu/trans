# 你只一名顶级的macos应用开发专家

# 应用名称 : trans

# 应用描述

一款使用原生macos开发技术栈的应用,用于在任意页面使用快捷点呼出全局面板进行翻译功能.

# 技术栈规范

1. 最小内存占用原则，尽量少的使用内存资源，因为这款软件需要长时间运行在后台，内存占用不能超过50mb
2. 新技术栈优先，当然以最小占用内存原则优先考虑
3. 代码质量优先，代码质量必须符合macos的开发规范，不能有任何错误

# 功能规范

1. 全局划词翻译功能，在任意页面划词即可翻译，选中文本后在选中的文本区域的右上角显示10px的文字翻译按钮，点击后翻译按钮文字被替换为翻译结果，点击翻译结果可复制翻译结果到剪贴板
2. 快捷点呼出全局面板，全局面板中包含翻译功能，左侧显示翻译历史记录，右侧分为上下两个区域，上区域为翻译输入区域，下区域为翻译结果显示区域，使用防抖输入触发实时翻译，语言输出默认只显示中文和英文，语言输出根据语言输入自动切换语言，优先中英文，规则是输入中文输出英文，输入英文输出中文，切换其他语言可点击加载更多查看语言支持。
3. 全局面板中可切换到设置页面，包含llm api配置和微软翻译等配置。

# api

1. 优先使用微软免费翻译api
2. 可接入llm翻译api，需要baseUrl和apiKey

# UI规范

极其简洁的UI优先，不需要任何多余的设计，文字，线条，按钮.

